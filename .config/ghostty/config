# vi:ft=conf
#
# https://github.com/ghostty-org/ghostty/blob/main/src/config/Config.zig

clipboard-read = allow

copy-on-select = clipboard

cursor-style = block
cursor-style-blink = true

font-family = JetBrainsMono Nerd Font Mono
font-size = 13

selection-invert-fg-bg = true

shell-integration-features = no-cursor

theme = dracula

# When using tmux on MacOS the titlebar will always display "tmux a"
# (or whatever command was used to enter the session). Override the default
# style with a generic title so things look cleaner.
title =  👻 Ghostty
macos-titlebar-style = tabs
