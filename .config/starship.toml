# ~/.config/starship.toml

# Don't print a new line at the start of the prompt
add_newline = false

[username]
show_always = true

[hostname]
ssh_only = false
disabled = false
prefix = "⟪"
suffix = "⟫"
style = "yellow"

[character]
symbol = "➜"
use_symbol_for_status = true
vicmd_symbol = "←"
